# Canvas Utilities (`utils/canvas/`)

This directory contains utilities for canvas rendering and UV coordinate analysis, split into two functional areas:

- **`drawing/`** - Pure canvas rendering functions (no Three.js dependency)
- **`uv-mapping/`** - Three.js geometry analysis and UV coordinate mapping
